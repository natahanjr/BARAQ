fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
# PostgreSQL backend for fleet-scale deployments. Only strictly required when
# SENTINEL_DATABASE_URL points at a postgres:// URL.
psycopg[binary]>=3.1
# Schema migrations (alembic upgrade head on deploy; alembic revision --autogenerate on model changes).
alembic>=1.13
# Field-level AES-256-GCM encryption at rest (backend.crypto). Present since
# Phase 1 hardening; only active when SENTINEL_ENCRYPT_AT_REST=1 or FROZEN.
cryptography>=41.0
pydantic>=2.6
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
reportlab>=4.1
psutil>=5.9
pywin32>=306; platform_system == "Windows"
pytest>=8.0
httpx>=0.27
