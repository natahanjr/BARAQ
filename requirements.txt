fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
# PostgreSQL backend for fleet-scale deployments. Only strictly required when
# BARAQ_DATABASE_URL points at a postgres:// URL.
psycopg[binary]==3.3.4
# Schema migrations (alembic upgrade head on deploy; alembic revision --autogenerate on model changes).
alembic==1.19.1
# Field-level AES-256-GCM encryption at rest (backend.crypto). Present since
# Phase 1 hardening; only active when BARAQ_ENCRYPT_AT_REST=1 or FROZEN.
cryptography==50.0.0
pydantic==2.13.4
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
# Gradient-boosted second opinion for the ML detector (auto-selected on
# corpora >= 400 labelled rows; sklearn random forest below that).
xgboost==3.4.1
matplotlib==3.11.1
reportlab==5.0.1
psutil==7.2.2
pywin32>=306; platform_system == "Windows"
pytest==9.1.1
pytest-asyncio==0.28.1
httpx==0.28.1
pydantic-settings==2.15.0
openai>=1.30
